package org.literacyapp.contentprovider.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import org.literacyapp.contentprovider.model.content.multimedia.JoinVideosWithWords;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "JOIN_VIDEOS_WITH_WORDS".
*/
public class JoinVideosWithWordsDao extends AbstractDao<JoinVideosWithWords, Long> {

    public static final String TABLENAME = "JOIN_VIDEOS_WITH_WORDS";

    /**
     * Properties of entity JoinVideosWithWords.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property VideoId = new Property(1, long.class, "videoId", false, "VIDEO_ID");
        public final static Property WordId = new Property(2, long.class, "wordId", false, "WORD_ID");
    }


    public JoinVideosWithWordsDao(DaoConfig config) {
        super(config);
    }
    
    public JoinVideosWithWordsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"JOIN_VIDEOS_WITH_WORDS\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"VIDEO_ID\" INTEGER NOT NULL ," + // 1: videoId
                "\"WORD_ID\" INTEGER NOT NULL );"); // 2: wordId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"JOIN_VIDEOS_WITH_WORDS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, JoinVideosWithWords entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getVideoId());
        stmt.bindLong(3, entity.getWordId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, JoinVideosWithWords entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getVideoId());
        stmt.bindLong(3, entity.getWordId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public JoinVideosWithWords readEntity(Cursor cursor, int offset) {
        JoinVideosWithWords entity = new JoinVideosWithWords( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // videoId
            cursor.getLong(offset + 2) // wordId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, JoinVideosWithWords entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setVideoId(cursor.getLong(offset + 1));
        entity.setWordId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(JoinVideosWithWords entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(JoinVideosWithWords entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(JoinVideosWithWords entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
